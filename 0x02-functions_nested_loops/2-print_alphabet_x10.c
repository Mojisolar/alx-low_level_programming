#include "main.h"
/**
 * print_alphabet_x10 - Make 10 times the alphabet
 * Return: void
 */
void print_alphabet_x10(void)
{
	char p;
	int b = 0;
	while (b <= 0)
	{
		for (p = 'a'; p <= 'z'; p++)
		{
			_putchar(p);
		}
		b++;
		_putchar('\n');
		}
}
